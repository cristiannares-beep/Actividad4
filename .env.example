# Puerto del servidor
PORT=3000

# URI de MongoDB (local o Atlas)
MONGODB_URI=mongodb://localhost:27017/gestion-productos

# Secreto para firmar JWT (cambiar en producción)
JWT_SECRET=tu_secreto_muy_seguro_aqui

# Tiempo de expiración del token (ej: 24h, 7d)
JWT_EXPIRES_IN=24h
